# 위스티아 홈페이지 작업 지침

이 파일은 홈페이지 작업의 기준과 인계 기록이다

## 매 작업의 시작과 종료

1. 작업을 시작하기 전에 이 파일 전체를 읽고, 현재 화면·소스·Git 상태를 확인한다
2. 최근 사용자 지시가 이 파일과 다르면 최근 지시를 우선한다
3. 작업을 마치기 전에 실제 모바일 화면과 주요 클릭 동작을 확인한다
4. 작업 결과, 검증 내용, 남은 문제를 아래 작업 기록에 날짜와 함께 갱신한다
5. 사용자가 직접 배포·푸시하므로 명시적인 요청 없이 원격으로 푸시하지 않는다

## 디자인·콘텐츠 기준

- 모바일 화면을 우선한다, 특히 320px과 390px에서 가로 넘침과 버튼 가림을 확인한다
- 위스티아의 흰색·실버 기반, 얇은 선, 넉넉한 여백을 유지한다, 실제 고객 사진을 쓰는 섹션과 첫 화면의 생성 이미지 카드는 구분해 관리한다
- PPT처럼 큰 빈 상자만 반복하지 않는다, 사진·타이포그래피·선택 상태로 위계를 만든다
- 과도한 효과보다 부드럽고 짧은 전환을 쓴다, 움직임 감소 설정을 존중한다
- 가격·혜택·제작 기한·고객 안내 문구는 임의로 바꾸지 않고 현재 근거를 확인한다
- 이전에 삭제하라고 한 구성이나 문구를 되살리지 않는다
- 모든 상품 상세의 타사 비교표는 사용하지 않는다, 대신 공통 `위스티아의 장점` 다섯 항목을 표시하고 타사 상호명은 화면과 코드에 노출하지 않는다
- 기존 기능과 화면 이동을 보존하고, 수정 대상 밖의 파일은 함부로 지우지 않는다
- 첫 화면 네 서비스 카드는 사용자가 제공한 토끼·분홍 새 캐릭터 레퍼런스를 따르는 이미지로 유지한다, 실제 제작 현장 사진을 보여주는 상세 섹션과 혼동하지 않는다
- AR 축가는 인원 제한 없는 녹음 1시간 12만원·2시간 16만원으로 구분한다, `가사 영상 추가` 옵션에는 `assets/img/song-options/lyric-video-v2.webp` 이미지를 함께 보여주며 두 시간제 모두 4만원이다
- 녹음 메이킹은 1인 22만원·2인 28만원, 프로포즈는 스토리형 기본 29만원·메이킹 변경 시 22만원, 듀엣 식전 영상은 스토리형 기본 35만원·메이킹 변경 시 28만원으로 계산한다, 선택에 따라 최종 가격을 즉시 갱신한다
- 녹음 메이킹 필름의 상세 상단 인원 선택 카드에는 가격 없이 `1인`과 `2인`만 적는다, 실제 금액은 가격 계산기에서 확인하게 한다
- 첫 화면 서비스 카드는 `AR 축가 사전녹음` → `축가 녹음 메이킹 필름` → `듀엣 식전 스토리 필름` → `프로포즈 · 답프로포즈` 순서와 명칭을 유지하며, 카드·메뉴·계산기의 서비스명에는 번호를 표시하지 않는다
- 상품 구성은 사진이 절반을 차지하는 박스 대신, 녹음·촬영 시간과 수작업 음정·박자 보정, 믹싱, 마스터링을 아이콘 카드로 보여준다, 당연한 `영상 편집` 카드는 영상 상품 전체에서 빼고 스토리형에는 해당하는 뮤직비디오 클립·하객 메시지·인터뷰·전하는 편지를 표시한다
- 입장곡 추가는 신부·신랑 두 항목을 별도 체크박스로 표시하고 각각 4만원으로 계산한다, AR·녹음 메이킹·듀엣 식전·프로포즈의 가격 화면에 동일하게 적용한다
- 후기 카루셀은 쇼케이스처럼 끊김 없이 자동 순환한다, 모션 감소 환경에서는 속도를 낮추되 카드가 순간 이동하지 않게 한다
- 듀엣 식전 영상과 프로포즈 상세는 스토리형 단일 페이지로 유지한다, 녹음 메이킹 필름은 별도 상세 탭이 아니라 `−7만원` 변경 옵션으로 안내한다
- 가격 계산기의 추가 수정 규정은 `3회까지 무료 · 4회차부터 회당 10,000원` 안내 문구로만 표시한다, 수정 횟수 선택 버튼이나 예상 가격 가산 항목으로 만들지 않는다
- 가격 화면의 `이 가격으로 상담 받기!`는 선택한 서비스·옵션·최종 금액을 먼저 클립보드에 복사하고 안내 팝업만 연다, 팝업의 `확인했습니다! 카카오톡으로 이동`을 누르기 전에는 카카오톡으로 이동하지 않는다
- 첫 화면 AR 축가 캐릭터 사진은 카드의 옅은 블루·쿨 아이보리 배경과 맞춘다, 과장된 표정·떠다니는 음표와 하트 같은 유아틱한 장식은 피한다
- 소개 페이지는 569px을 포함한 좁은 화면에서 글과 사진을 한 열로 정렬한다, `위스티아가 드리는 약속` 독립 페이지와 메뉴는 사용하지 않는다
- 메뉴와 질문 페이지에는 `QnA` 대신 `자주 묻는 질문`을 사용하고 질문은 방문·녹음·제작 및 수정·영상 상품·AR 축가로 분류한다

## 확인 방법

- 수정한 JavaScript는 `node --check`, 변경 파일은 `git diff --check`로 확인한다
- 로컬 미리보기에서 모바일 카드의 내용·사진·클릭 이동과 가로 넘침을 확인한다
- 주요 화면은 가능하면 320px, 390px, 768px, 1440px에서 확인하되 모바일 문제를 우선 해결한다
- 최종 결과는 실제로 열 수 있는 로컬 파일이나 미리보기 링크로 안내한다

## 작업 기록

### 2026-09-23

- 사용자가 제공한 홈페이지 전체 ZIP을 작업 폴더에 복사하고 각 파일 해시 일치를 확인했다
- 첫 화면 서비스 선택 영역을 실제 사진이 들어간 모바일 카드형으로 개편했다
- 세 카드 높이를 통일하고, 작은 화면에서 문의 버튼이 카드를 가리지 않도록 원형으로 축소했다
- `js/app.js`, `css/home-finder.css`, `index.html`을 수정했다, ZIP에 담긴 `hcs-edits.js`는 제목을 임의로 바꾸는 편집기 산출물이므로 홈페이지에서는 불러오지 않는다
- 390px과 320px에서 화면을 확인했고 사전 AR 축가·프로포즈 카드의 페이지 이동을 확인했다
- `node --check js/app.js`와 `git diff --check`를 통과했다
- ZIP을 풀었던 `tmp/incoming-20260923-082332-729`은 원본 ZIP과 홈페이지 반영본이 따로 있는 임시 복사본이다, 정리 시 이 경로만 정확히 확인해 삭제한다
- 서비스 카드 세 장의 높이와 사진 영역을 동일하게 맞추고 각각 옅은 블루·샴페인·로즈 색상을 적용했다
- 카드 제목을 `AR 축가 사전 녹음`, `식전 / 축가 영상`, `프로포즈 / 답프로포즈`로 확정했다
- 320px과 390px에서 문구·색상·가로 넘침을 확인했고, 식전 / 축가 영상 카드의 이동을 확인했다
- `node --check js/app.js`와 `git diff --check`를 다시 통과했다
- 첫 화면 카드 사진 3장을 내장 이미지 생성 도구로 새로 제작했다, AR 축가와 프로포즈는 여성 단독, 식전·축가 영상은 남녀 커플의 스튜디오 녹음 장면이다
- 원본 PNG와 웹 표시용 600×800 JPG를 `assets/img/finder-ai/`에 보관하고, 카드에서는 JPG를 불러온다
- 첫 화면 카드에 한때 넣었던 `AI 이미지` 표시는 사용자 요청에 따라 제거했다, 카드 안에 다시 표시하지 않는다
- 카드의 작은 대각선 화살표를 `자세히 보기 →` 표시로 바꾸고, 실제 클릭 시 상품 상세로 이동하는지 확인했다
- 320px과 390px 모바일 화면에서 이미지 비율, 문구, 카드 높이, 가로 넘침을 확인했다
- 첫 화면 카드 사진 3장을 인물보다 녹음실 공간이 넓게 보이는 먼 구도의 새 버전으로 교체했다, AR 축가와 프로포즈는 여성 단독이며 식전·축가 영상은 남녀 커플이다
- 이전 사진은 보존하고 새 PNG와 웹 표시용 JPG를 `assets/img/finder-ai/`에 `-wide` 이름으로 추가했다
- 카드의 `AI 이미지` 문구를 삭제하고 320px·390px 실제 모바일 화면에서 사진과 문구가 카드 밖으로 잘리지 않는 것을 확인했다, 768px·1440px에서 가로 넘침이 없었으며 AR 카드 상세 이동을 확인했다
- 모든 상품 상세에서 하단 고정 금액·상담 바와 푸터의 중복 상담 버튼을 제거하고, 상세 화면의 상단 가격 이동 띠도 제거했다
- 우측 카카오채널 버튼은 보관된 `assets/img/kakao-talk.png` 이미지로 교체했다, 가격 계산기에서는 중복을 피하도록 우측 버튼 대신 계산 영역의 문의 버튼을 사용한다
- AR 제작과정의 16:9 원본 사진을 모바일에서 카드 상단 전체 폭으로 보여주어 잘림을 없앴다, 1단계와 6단계를 실제 화면에서 확인했다
- 메뉴의 `AR 축가 사전녹음`은 다른 메뉴와 같은 진하기로 표시하고, 모든 상품 가격 계산기 제목을 `얼마일까?`로 변경했다
- 6개 상품 가격 화면에 `지금 내 가격은?`과 `카카오채널 1:1 문의하기`를 같은 형태로 적용했다, 옵션과 이벤트 선택 때 금액이 즉시 변하는 것을 확인했다
- 320px 가격 화면에서 가로 넘침과 버튼 잘림이 없는 것을 확인했다
- AR 상품 상세의 일반 제작 비교표를 WISTIA 실제 제작 과정 중심의 2열 표로 교체했다, 타 업체의 제작 방식을 단정하는 문구는 제거했다
- 1인 AR에는 90분 녹음, 마디·구간별 녹음, 1:1 디렉팅, 수작업 튠, 믹싱·마스터링과 기본가 12만원을 명시했다
- 2인 AR에는 파트·화음별 녹음과 동일한 제작 단계, 기본가 16만원을 표시했다, 녹음 시간이 확인되지 않아 90분이라고 쓰지 않았다
- `node --check js/app.js`와 `git diff --check`를 통과했고, 320px·390px 실제 화면에서 비교표와 가로 넘침을 확인했다
- 사용자 정정에 따라 1인 AR의 위스티아 단독 표를 헤마 스튜디오·파도뮤직 PRO·WISTIA 3개 실제 상품 비교 카드로 대체했다, 이전 단독 표는 최종안이 아니다
- 헤마의 1시간 14만원·3시간 22만원 및 보컬 보정·믹싱·마스터링 포함은 사용자가 제공한 정보이며 공식 공개 가격은 독립 확인되지 않았다, 화면에 확인 한계를 명시했다
- 파도뮤직 PRO의 50분·16만원·보컬 튜닝·1:1 디렉팅은 공식 상품 페이지를 확인했다, 믹싱·마스터링은 불포함이라고 단정하지 않고 해당 상품 안내에 별도 표기가 없다고 적었다
- WISTIA 1인 AR은 90분·12만원과 마디·구간별 녹음, 1:1 디렉팅, 수작업 튠, 믹싱·마스터링 포함을 명시했다, 2인 AR의 별도 구성 표는 유지했다
- `node --check js/app.js`와 `git diff --check`를 통과했고, 320px·390px 실제 모바일 화면에서 3개 비교 카드와 가로 넘침 여부를 확인했다
- 최신 사용자 정정에 따라 3개 비교 카드는 제거했다, 최종안은 `타 상품 / 비교 항목 / WISTIA` 3열 표이며 타사 상호명은 화면과 홈페이지 코드에서 모두 제거했다
- 비교표에는 60분·180분·50분 상품의 녹음 시간·가격·포함 항목을 상호명 없이 구분해 적었다, 확인되지 않은 항목은 단정하지 않는다
- 320px·390px 실제 화면에서 3열 표와 가로 넘침 여부를 확인했고 `node --check js/app.js`, `git diff --check`를 통과했다
- 최신 확정안: 1인 AR의 3열 비교표는 가격 행과 1:1 디렉팅 행 없이 `녹음 시간 / 녹음 방식 / 보컬 보정 / 믹싱·마스터링` 4개 행만 표시한다, 타사 상호명을 노출하지 않는다
- 첫 화면 서비스는 `AR 축가 사전 녹음`, `식전 / 축가 영상`, `프로포즈 필름`, `녹음 메이킹 필름` 4개로 분리했다
- AR은 1인·2인 탭을 유지하고, 영상 상세의 교차 탭은 제거했다, 식전·축가 영상은 하나의 안내 페이지에서 두 상영 시점을 설명한다
- 첫 화면 4개 카드 이동을 실제 클릭해 확인했다, 320px·390px에서 가로 넘침이 없고 `node --check js/app.js`와 `git diff --check`를 통과했다
- 첫 화면 네 번째 `녹음 메이킹 필름` 카드 사진을 여성 보컬과 촬영 카메라가 함께 보이는 새 생성 이미지 `assets/img/finder-ai/recording-making-woman-v1.png`로 교체했다, 상품 상세의 기존 제작 사진은 변경하지 않았다
- 320px·390px 실제 화면에서 여성 얼굴과 녹음 장비가 잘리지 않는지, 이미지 로딩·카드 이동·가로 넘침을 확인했다
- 2026-09-23 가격 확정: AR 1인 12만원·2인 16만원과 가사 영상 +4만원, 녹음 메이킹 1인 22만원·2인 28만원, 식전·축가 스토리형 2인 35만원, 프로포즈 녹음 메이킹 22만원·스토리형 29만원을 계산기에 반영했다
- 영상 상품의 입장곡은 인당 +4만원(프로포즈는 신부 입장곡 +4만원), 3일 이내 빠른 작업은 +3만원, 수정 3회까지 무료·4회차부터 회당 +1만원으로 반영했다, 1인 메이킹에서는 입장곡 1명분까지만 선택된다
- 녹음 메이킹 제작 과정에서 같은 사진을 모든 단계에 반복하던 설정을 7장의 단계별 이미지로 바꿨다
- 실제 화면에서 AR 가사 영상 16만원, 2인 메이킹 옵션·이벤트 합계 37만원, 식전·축가 35만원, 프로포즈 메이킹 22만원·스토리형 옵션 합계 37만원을 확인했다, 320px 모바일에서 가로 넘침 없이 옵션을 확인했다
- 가격 화면의 사전 정보 입력을 모두 제거하고 01 기본 가격(서비스·인원·영상 구성), 02 추가 옵션, 03 이벤트, 04 따라다니는 최종 가격 말풍선 순서로 바꿨다, CTA는 `이 가격으로 상담 받기!`로 확정했다
- 가격 화면은 진입한 상품을 기본 선택으로 보여주며 서비스·인원·프로포즈 영상 구성을 바꾸면 기본가와 최종가가 즉시 바뀐다, 카카오 상담에 복사되는 내용에서도 사전 정보 항목을 제거했다
- 한 페이지의 재생 음원이 겹치지 않도록 AR 미리듣기·비포/애프터 비교·영상 재생의 포커스를 통합했다, 비포/애프터 → AR, AR → 비포/애프터 순서에서 이전 재생이 멈추는 것을 확인했다
- 320px 화면에서 사전 정보 요소 0개와 가로 넘침 0을 확인하고, 390px 화면에서 선택 UI와 최종 가격 말풍선 배치를 확인했다
- 최종 파일을 새로 불러온 뒤 AR 2인 16만원, 메이킹 2인 28만원, 식전·축가 스토리형 35만원, 프로포즈 스토리형 29만원이 진입 경로별 기본값으로 선택되는 것을 재확인했다
- 첫 화면 네 카드의 사진을 사용자 제공 캐릭터 레퍼런스 기반의 새 이미지로 교체했다, 원본 참고 이미지는 수정하지 않고 새 PNG 네 장과 모바일 표시용 600px JPG 네 장을 `assets/img/finder-ai/`에 보관한다
- AR 가격 화면의 가사 영상 추가 옵션에 기존 제작 이미지 `lyric-video-v2.webp`를 표시했다
- 녹음 메이킹 1인·2인, 프로포즈 녹음 메이킹·스토리형, 식전 / 축가 영상 스토리형·녹음 메이킹을 상세 상단과 가격 화면에서 각각 선택하도록 분리했다
- 373px 모바일 미리보기에서 첫 화면 캐릭터 JPG 네 장의 로딩과 가로 넘침 없음, 메이킹 1인 22만원·2인 28만원, 식전·축가 스토리형 35만원·녹음 메이킹 28만원, 프로포즈 메이킹 22만원·스토리형 29만원의 선택·가격 이동을 확인했다
- 가사 영상 옵션의 기존 이미지 로딩과 AR 1인 기본가 12만원에서 옵션 선택 시 16만원으로 바뀌는 동작을 확인했다
- 상세 상단의 식전 / 축가 영상·프로포즈·녹음 메이킹 선택 카드에서 가격 표시를 모두 제거했다, 녹음 메이킹 카드는 `1인`·`2인`만 표시한다
- 모바일 미리보기에서 세 상품 카드 문구와 가로 넘침 없음, 프로포즈 `스토리형` 선택 후 가격 계산기 기본가 29만원 유지 여부를 확인했다
- 첫 화면 1번 AR 축가 이미지를 기존 캐릭터와 녹음 구도는 유지하면서 표정을 차분하게 하고 음표·하트를 뺀 따뜻한 스튜디오 톤의 `character-ar-vocal-v2.jpg`로 교체했다, 생성 원본 PNG도 같은 위치에 보관했다
- 373px 모바일 미리보기에서 새 이미지 로딩·카드 잘림과 가로 넘침 없음, 1번 카드의 AR 상세 이동을 확인했다
- 첫 AR 카드의 분홍 배경이 블루 카드와 어긋나던 문제를 수정해, 기존 토끼·마이크 구도를 유지한 옅은 블루 스튜디오 이미지 `assets/img/finder-ai/character-ar-vocal-v3.jpg`로 교체했다, 생성 원본 PNG도 함께 보관했다
- 1인·2인 AR 현장 영상은 브라우저 자동재생 정책상 음소거 상태로 시작하며, 명시적인 `소리 켜기` 버튼을 누르면 실제 MP4의 AAC 오디오가 재생되도록 했다, 다른 재생 음원은 기존 공통 재생 제어로 멈춘다
- 모든 통합 상품 상세의 제작 과정을 좌우 슬라이더 대신 세로 번호 아코디언으로 바꿨다, 처음에는 1단계가 열리며 다른 단계를 누르면 기존 열린 단계는 닫힌다, 과정 사진은 원본 비율을 유지한다
- 상품 상세 비교표를 전 상품 동일한 `타 상품 / 비교 항목 / WISTIA` 3열로 통일했다, 1인 AR에 이미 확정한 비교 문구는 유지하고 다른 상품에서 검증되지 않은 타사 제작 방식은 `상품별 확인`으로 표시했다, 타사 상호명은 노출하지 않는다
- 상세 섹션의 화이트·차콜 배경 리듬과 공통 정렬 폭을 맞추고, FAQ 아래 버튼은 전체 폭의 `이벤트 할인 가격 보기`로 바꿨다
- 메뉴에 `보컬 보정 전 후 비교 보기`를 추가해 별도 비교 청취 화면으로 연결했다
- `node --check js/app.js`와 `git diff --check`를 통과했고, 모바일에서 1인·2인 영상 음소거 해제 후 재생, 7단계 아코디언, 식전 영상 공통 비교표, 메뉴 이동, 첫 카드 새 이미지와 320px·390px 가로 넘침 없음 확인했다

### 2026-09-24

- 최신 확정 상품 기준에 따라 AR 축가를 인원제가 아닌 1시간 12만원·2시간 16만원, 인원 제한 없음으로 전환했다, AR 상세 탭·헤드라인·메뉴·가격 계산기·상담 복사 문구에 적용했다
- 전 상품 상세에 이미지와 구성 항목이 나란히 놓인 가로형 상품 구성 박스를 추가했다, AR은 녹음 60분·120분, 메이킹 1인은 녹음·촬영 총 90분, 메이킹 2인과 듀엣 식전 영상은 총 180분, 프로포즈는 총 90분으로 안내한다
- 가로형 구성 박스에는 수작업 음정·박자 보정, 믹싱, 마스터링, 영상 상품의 편집을 명시하고, 하단에 스토리형·녹음 메이킹의 차이 및 직접 촬영 영상 삽입 가능 조건을 설명한다
- 기본 안내를 전 상품 상세에 반영했다, 표시 시간은 안내·연습·부가 시간 포함, 수정 3회 무료와 4회차부터 회당 1만원, 가능한 일정의 3일 이내 작업 +3만원이다
- AR의 가사 영상 +4만원, 신부·신랑 입장곡 인당 +4만원을 계산기에 반영했다, 녹음 메이킹 1인 22만원·2인 28만원과 스토리형 업그레이드 +7만원, 식전 영상 스토리형 35만원·메이킹 28만원, 프로포즈 스토리형 29만원·메이킹 22만원을 확인했다
- 320px·390px에서 새 가로형 구성 카드의 이미지·텍스트와 가로 넘침 없음, AR 2시간+가사 영상+입장곡 1명 24만원, 2인 메이킹 스토리형 업그레이드 35만원을 실제 화면에서 검증했다
- 첫 화면 카드의 번호·이름·순서를 최종 확정안인 AR 축가, 축가 녹음 메이킹 필름, 듀엣 식전 영상, 프로포즈로 바로잡고 메뉴와 계산기의 서비스명도 맞췄다
- 상품 구성의 사진 중심 가로 박스를 아이콘형 가로 카드로 변경했다, 모바일에서는 읽기 쉬운 2열로 접히며 AR 60분·120분과 영상 상품의 합산 녹음·촬영 시간을 표기한다
- 듀엣 식전 영상과 프로포즈 상세의 영상 구성 탭을 제거해 스토리형 단일 페이지로 만들고, 메이킹 변경 조건을 설명하는 −7만원 옵션을 가격 계산기로 연결했다
- 추가 수정은 가격 계산기의 수량 선택과 금액 가산을 제거하고 안내문으로만 남겼다
- 320px·390px·768px·1440px 식전 영상 상세에서 가로 넘침과 영상 구성 탭 0개를 확인했고, 320px AR 구성 카드와 식전·프로포즈의 메이킹 안내를 실제 화면으로 확인했다
- 첫 화면 네 카드의 이미지 로딩·이동 경로를 각각 확인했다, 기본가는 AR 12만·16만원, 축가 녹음 메이킹 22만·28만원, 듀엣 식전 35만원, 프로포즈 29만원이며 메이킹 변경 시 각각 28만원·22만원으로 계산된다
- AR 2시간에 가사 영상과 입장곡 1인 옵션을 추가한 24만원을 확인했고, 전 상품 가격 계산기에서 추가 수정 수량 선택이 없는 것을 확인했다, 브라우저 오류 로그 없음
- 구성 카드 아이콘을 절제된 단색 선형 SVG로 정리하고 320px 실제 화면에서 4개 아이콘 로딩과 가로 넘침 없음, `node --check js/app.js` 및 `git diff --check` 통과를 재확인했다
- 카드 제목에도 `01`부터 `04`까지 번호를 직접 표기하고 320px·390px에서 네 제목과 사진 로딩 및 가로 넘침 없음, 최종 구문·차이 검사 통과를 확인했다
- 사용자 정정에 따라 첫 서비스 선택 화면의 단계 번호와 카드 제목·메뉴·가격 계산기 서비스명의 `01–04` 번호를 모두 제거했다, 기존 네 상품의 순서와 이동 경로는 유지한다
- 320px 첫 화면에서 카드 네 제목·영문 보조문구·단계 번호 비노출과 가로 넘침 없음, 390px에서 메이킹 카드 이동 및 메뉴·계산기 서비스명에 번호가 없는 것을 확인했다
- 고객 후기 카루셀의 마우스·키보드 초점 정지를 제거하고, 모션 감소 환경에서도 자동으로 한 장씩 순환하도록 수정했다
- 네 서비스명을 새 확정안으로 맞추고, 모든 가격 화면의 입장곡을 신부·신랑 각각 4만원 체크박스로 분리했다
- 모든 영상 상품 구성에서 `영상 편집` 카드를 제거했다, 듀엣 식전·축가 스토리형에는 뮤직비디오 클립·하객 메시지·인터뷰·전하는 편지를, 프로포즈 스토리형에는 뮤직비디오 클립·인터뷰·전하는 편지를 표시했다
- 메이킹에서 스토리형 업그레이드와 스토리형에서 메이킹 변경의 구성 차이를 각 3개 안내 문장으로 나누어 가격 화면과 상세 안내에 적용했다
- 390px에서 AR 1·2시간, 메이킹 1·2인, 식전·축가 스토리형, 프로포즈의 구성 카드를 각각 확인했고 전 상품 카드에 `영상 편집` 항목이 없으며 390px 가로 넘침도 없었다
- 모든 가격 화면에 신부·신랑 입장곡이 각각 보이는 것을 확인했다, AR 1시간에서 둘 다 선택 시 20만원, 메이킹 1인 스토리형 선택 시 29만원, 식전 메이킹 변경 시 28만원, 프로포즈 메이킹 변경 시 22만원으로 계산되었다
- 모션 감소가 켜진 브라우저에서 AR·식전·프로포즈 후기 카루셀의 자동 이동과 AR 후기 화살표 클릭 뒤 자동 이동 지속을 확인했다, 320px 옵션 설명 12px 및 가로 넘침 없음을 확인했다
- 후기 카루셀의 모션 감소 환경 이동을 0.58초 완만한 카드 전환으로 바꾸고 마지막 카드에서 첫 카드로 돌아갈 때의 순간 위치 보정을 추가했다, 320px 실제 브라우저에서 중간 프레임과 순환 경계를 확인했다
- AR 상세 전문가 소개 제목 위의 `WISTIA · BRAND STORY` 문구를 제거하고, 메뉴의 프로포즈 그룹을 보컬 보정 전후 비교 메뉴 위로 옮겼다
- 전 상품 계산기의 신부·신랑 입장곡 설명을 `입장곡 하이라이트 부분 녹음`으로 통일하고, AR 상세 설명에도 하이라이트 녹음을 명시했다
- 듀엣 식전·프로포즈 상세의 `−7만원` 메이킹 변경 카드를 가격 화면으로 이동하지 않는 안내문으로 바꿨다, 320px·390px 실제 화면에서 가로 넘침과 안내 카드 배치를 확인했다
- 메뉴 프로포즈 이동, AR·식전·프로포즈 가격 화면의 입장곡 문구, 상세 안내의 비링크 상태를 확인했으며 `node --check js/app.js`와 `git diff --check`를 통과했다
- 390px AR 가격 화면에서 신부·신랑 입장곡을 각각 선택하자 12만원에서 20만원으로 정확히 갱신되었고, 메뉴의 프로포즈 링크도 실제 상세 페이지로 이동했다
- 전 상품 상세의 비교표를 제거하고 녹음 방식·보컬 보정·추가 비용·상품 구성·가격의 `위스티아의 장점` 다섯 항목으로 교체했다, AR 1·2시간, 메이킹 1·2인, 듀엣 식전, 프로포즈 상세에서 다섯 항목과 비교표 0개를 확인했다
- 소개 페이지의 569px 두 열 폭 문제를 해결해 900px 이하에서 글과 사진을 한 열로 정렬했다, `위스티아가 드리는 약속` 메뉴와 페이지를 제거했다
- 메뉴의 `QnA`를 `자주 묻는 질문`으로 변경하고, 열린 메뉴 아이콘은 X 대신 세 줄 모두 기울어지는 형태로 바꿨다
- 제공된 FAQ 이미지 두 장의 질문 14개를 방문·녹음·제작 및 수정·영상 상품·AR 축가 다섯 분류로 정리했다, 질문 열림 동작과 320px·390px·569px·768px·1440px 주요 화면의 가로 넘침 없음을 확인했다
- 사용하지 않는 예전 AR 비교표 생성 코드와 동작 초기화도 제거했다, 최종 파일에서 JavaScript 문법·Git 차이 검사와 브라우저 오류 로그 0개를 확인했다
- 고객 후기 카루셀을 모션 감소 환경에서도 느린 연속 이동으로 바꾸고, 화살표를 누른 첫 프레임의 점프를 제거했다, 220ms 간격에서 일정한 이동과 수동 전환을 확인했다
- AR 비율을 슬라이더·키보드·휠로 바꿀 때 음원이 자동 재생되지 않고, 재생 중이었다면 멈추도록 수정했다, 재생 버튼 클릭 시에만 다시 들리는 것을 확인했다
- 후기와 인접한 밝은·어두운 섹션 경계에 세로 그라데이션을 적용하고 전문가 소개 제목 위 여백을 늘렸다, 320px·390px에서 가로 넘침 없이 배경과 여백을 확인했다
- `node --check js/app.js`, `node --check js/ar-ratio-cd.js`, `git diff --check`를 통과했다
- 사용자 정정에 따라 어두운 후기·비포애프터·제작과정 섹션을 단색으로 되돌리고, 인접한 밝은 섹션의 빈 여백에만 24px의 완만한 그라데이션을 두었다, 이전의 72px 회색 띠와 카드 뒤 그림자 느낌을 제거했다
- 320px·390px 실제 브라우저에서 경계와 가로 넘침 없음, 후기 다음 버튼 동작을 확인했다
- 가격 계산기 상담 흐름을 두 단계로 바꿨다, 첫 클릭에서 선택 내용이 복사되고 `상담양식이 복사되었습니다 / 카카오톡에 붙여넣기 해주세요!` 팝업만 표시되며, 팝업의 확인 버튼을 눌렀을 때에만 카카오톡 새 탭이 열린다
- AR 1시간 기본 12만원과 신부 입장곡 추가 후 16만원이 각각 복사 양식에 반영되는 것을 브라우저 클립보드에서 확인했고, 320px·390px 모바일 팝업과 자동 이동 없음, 확인 버튼 이동을 검증했다
- AR 상세의 `AR RATIO EXPERIENCE` 섹션을 전면 재설계했다, 색상 단계별 원형 그래픽을 레트로 턴테이블로 바꾸고 배경은 좌→우 흰색→옅은 회색의 무채색으로 정리했다, 참고 이미지의 색감은 사용하지 않는다
- 제목은 AR BENEFIT 인트로와 같은 타이포 토큰을 쓰고 `내 목소리에 맞게`를 다크 강조 박스로, `AR 비율을 골라보세요`를 둘째 줄로 표시한다
- 턴테이블은 기존 `wistia-disc.png`를 줄인 `assets/img/ar-ratio/wistia-disc-720.webp`와 WISTIA 로고 라벨 `wistia-label-logo.webp`를 사용한다, 다른 서비스 로고나 타인의 앨범 아트는 넣지 않는다
- 턴테이블을 누르면 재생·일시정지되고 LP 회전·톤암·레버가 함께 움직인다, 손가락 코치마크는 첫 조작 후 사라진다
- 플레이바는 얇은 한 줄 형태로 줄이고, 하단 LIVE/AR 비율 바는 46px 두께의 무광 트랙과 흰 원형 손잡이로 바꿨다, 30·50·70·100% 표시는 실제 손잡이 위치와 맞춘다
- 320px·390px에서 가로 넘침 0, 턴테이블 탭 재생/정지, 드래그 비율 변경(70→39%), 비율 바 클릭, 음소거 전환, 브라우저 오류 0개를 확인했고 `node --check`와 `git diff --check`를 통과했다
- 사용자 정정: AR 비율을 바꿔도 재생을 멈추지 않는다, 이전의 `비율 변경 시 재생 정지` 규칙은 폐기한다. 30·50·70·100% 음원 4개를 Web Audio로 동시에 재생하고 비율에 따라 인접 음원의 볼륨만 교차 조절한다, 다른 음원이 재생되면 `wistiaClaimPlayback`으로 AR도 멈춘다
- AR 섹션 세로 여백을 약 2/3로 줄이고, 설명문을 한 줄로, 강조 박스와 둘째 제목 줄 간격을 좁혔다
- 320px에서 설명문 한 줄 유지와 가로 넘침 0, 재생 중 비율 바 클릭·휠 조작 후에도 재생 지속, 다른 음원 재생 시 정지, 끝까지 재생 후 0:00 복귀를 확인했다
- 사용자 정정: AR 섹션 순서는 제목 → 비율 바 → 턴테이블 → 플레이바다, 소리 비율은 비율 바(드래그·클릭·마우스 휠·키보드)에서만 바뀌고 턴테이블은 눌러서 재생·일시정지만 한다, 손가락·점선 화살표 코치마크는 비율 바 위에 둔다
- AR 장점 섹션 하단과 후기 아래·장점 다섯 항목 섹션의 밝은↔어두운 경계를 24px 띠에서 64px 완만한 블렌드로 바꾸고, AR 장점 섹션을 전체 폭으로 펼쳐 좌우 흰 틈을 없앴다(`css/customer-fixes.css`)
- 전문가 소개의 영상 연출·사운드 완성 카드는 화면에 보이는 동안 4.5초마다 번갈아 펼쳐진다, 직접 누르면 9초, 마우스를 올리거나 포커스가 있으면 그동안 자동 전환을 멈춘다
- 390px에서 경계 블렌드 전체 폭과 가로 넘침 0, 턴테이블 드래그·휠로 비율이 바뀌지 않음, 비율 바 클릭·휠 후 재생 지속, 턴테이블 탭 정지, 전문가 카드 자동 전환(약 4.5초 간격)을 확인했다, `node --check`·`git diff --check` 통과
- 원격 최신 커밋 3개와 로컬 상담 팝업 커밋의 병합 충돌을 해결했다, `index.html`에는 최신 AR 스크립트 버전과 상담 팝업이 포함된 통합 `app.js` 버전을 적용하고 양쪽 작업 기록을 모두 보존했다
- 상담 팝업이 보이지 않는다는 재현은 오래 열어 둔 브라우저 탭이 예전 `app.js?v=224`를 계속 실행한 것이 원인이었다, 상세·가격 탭을 최신 `app.js?v=228`로 새로 불러온 뒤 상세 → 가격 → 상담 버튼 경로에서 복사 안내 팝업이 열리고 카카오톡 탭은 열리지 않는 것을 확인했다
- 갱신된 팝업을 사용자 브라우저에 열어 두었고, 320px·390px에서 팝업 가로 넘침이 없는 것을 다시 확인했다

### 2026-09-25

- Meta Pixel ID `2825959717777272`의 기본 코드를 `index.html`에 설치했다, 개발용 localhost에서는 실제 Meta 전송을 막고 공개 도메인에서만 초기화한다
- SPA 화면 렌더링 뒤 최초 및 실제 URL 변경마다 `PageView`를 한 번 보내며, 같은 URL에서 `route()`가 다시 호출되어도 중복 전송하지 않는다
- 카카오톡 상담 링크를 실제 클릭할 때 브라우저 `Contact`와 `/api/meta-contact`의 서버 `Contact`에 동일한 이벤트 ID를 사용하도록 연결했다, 가격 상담 버튼의 복사 안내 팝업과 카카오톡 이동 순서는 유지한다
- Conversions API 액세스 토큰은 코드에 저장하지 않고 Vercel 서버 환경변수 `META_CONVERSIONS_ACCESS_TOKEN`으로 받는다, 토큰이 없어 현재 서버 실전송은 대기 상태다
- `tests/meta-events.test.mjs`에서 PageView 중복 방지, 카카오 링크 Contact, 브라우저·서버 이벤트 ID 일치, 서버 요청 검증을 통과했다, `node --check`와 `git diff --check`도 통과했다
- 로컬 320px·390px 가격 화면에서 가로 넘침이 없고 상담 버튼이 복사 안내 팝업만 여는 것을 확인했다, 공개 `https://wistiahp.vercel.app/`은 접속되지만 이번 픽셀 코드가 아직 반영되지 않은 상태임을 확인했다
- 이번 작업은 원격 푸시·배포를 수행하지 않았다, 배포 후 Meta 테스트 이벤트에서 PageView와 Contact 실수신 및 브라우저·서버 중복 제거를 확인해야 한다
- 모든 상품 상세의 FAQ 아래 `이벤트 할인 가격 보기` 버튼을 브랜드 옐로와 진한 원형 화살표로 통일하고, 모바일에서는 페이지 좌우 끝까지 펼쳤다, 버튼 아래의 불필요한 흰 여백도 제거했다
- 390px에서 AR 1·2시간, 메이킹 1·2인, 듀엣 식전·축가 영상, 프로포즈 상세의 동일한 버튼 색상·전체 폭·가로 넘침 없음을 확인했다, 320px 화면과 AR·식전 영상 버튼의 가격 계산기 이동도 확인했다
- 공통 `이벤트 할인 가격 보기` 버튼에 노란 입체 그라데이션·하단 두께감·돌출 원형 화살표를 적용하고 화살표 주위 파티클이 짧게 터지도록 했다, 모션 감소 설정에서는 움직임을 끄고 파티클을 정적으로 표시한다
- 320px AR과 390px 프로포즈 상세에서 전체 폭·가로 넘침 없음·가격 계산기 이동을 확인했고, 모션 감소가 켜진 브라우저에서 정적 파티클 상태와 새 CSS 버전 로딩을 확인했다
- 우측 하단 카카오 단일 아이콘을 입체형 `가격 보기`·`1:1 문의` 두 버튼으로 바꿨다, 가격 보기는 현재 상품의 계산기로 연결하고 문의는 기존 카카오채널 주소를 유지한다, 계산기 화면과 열린 메뉴에서는 중복 노출을 막았다
- 320px·390px에서 AR 상세의 버튼 문구·그림자·가로 넘침 없음과 가격 계산기 이동을 확인했고, 프로포즈 상세에서는 해당 상품 계산기 연결, 첫 화면에서는 두 버튼의 배치를 확인했다
- 후기 카루셀의 반복 경계에서 목표 위치가 표시 위치보다 먼저 초기화되어 잠깐 빈 배경이 보이던 문제를 고쳤다, 실제 카드 묶음 폭으로 화면 위치만 순환시키고 원본·복제 후기 이미지는 미리 불러오게 했다
- 우측 하단 `가격 보기`와 `1:1 문의`를 동일한 너비·높이로 맞췄다, 320px 화면에서 둘 다 137×51px이며 후기 원본·복제 이미지 22장이 모두 로드된 상태를 확인했다
- 390px에서는 두 버튼 모두 147×55px, 가로 넘침과 브라우저 오류가 없음을 확인했다, 후기 다음 버튼과 반복 위치를 확인하고 `node --check js/app.js`·`git diff --check`를 통과했다

### 2026-09-25

- 사용자가 확정한 새 캐릭터 장면표를 우선 적용하기 위해 최신 원격 `main`을 별도 작업 폴더에 복제하고 `codex/wistia-guide-birds` 브랜치에서 작업했다. 전역 마우스 추종은 이후 작업으로 남긴다.
- 홈·상품 상세·비포애프터·가격·소개·FAQ·푸터에 클릭을 가로채지 않는 새 가이드 레이어를 붙였다. 1인 상품은 크림색 한 마리, 2인·듀엣·웨딩·프로포즈는 크림색과 회색 두 마리로 표시한다.
- 투명 PNG 포즈, 애니메이션 WebP, SVG 장식 49개를 `assets/guide/`에 넣고 장면별 스크롤 위치·카드 안 포인터/터치·가격 합계·음원 재생·모션 감소 설정을 연결했다.
- Edge 브라우저 기반 테스트에서 홈 320px, 듀엣 390px, 비포애프터, 가격 CTA, 카드 터치와 320px 가격 패널 비가림을 확인했다. 기존 가격 CTA는 `#/event/solo`로 이동했다. 최종 구문·차이 검사와 전체 라우트 확인 결과는 PR에 기록한다.
- 운영 배포 뒤 사용자 스크린샷에서 새가 중앙 본문 캔버스(최대 500px) 밖으로 나가거나 제작 과정 카드 내용을 가리는 오류를 확인했다. 좌표와 캐릭터 크기의 기준을 브라우저 전체가 아닌 실제 `#app` 캔버스로 고쳤다.
- 섹션 이동 때 새가 텍스트 위를 가로지르던 좌표·크기 전환은 숨김→새 위치 등장·확대로 바꾸고, 제작 과정의 두 새는 제목 위 오른쪽 여백에 배치했다. 홈·비포애프터·제작 과정의 390px/960px 화면에서 캔버스 포함, 글·버튼·열린 카드 비가림을 측정했고 브라우저 테스트 15개를 통과했다. 열린 과정 카드만 보이는 위치에서는 새를 숨긴다.
- 최신 사용자 요청에 따라 장면별 이동·표정·두 마리 연출을 제거하고, 크림색 새 한 마리만 전체 페이지에서 마우스를 따라다니게 바꿨다. 새는 `#app` 경계 안에 머물고 텍스트·버튼·영상과 겹치지 않는 가까운 위치를 찾으며, 안전한 공간이 없거나 터치 입력일 때는 숨긴다. 새 추종 브라우저 테스트 5개, 실제 홈·상세의 390px·960px 비가림 확인, `node --check`와 `git diff --check`를 통과했다.
- 사용자 피드백으로 AR 비율 슬라이더 같은 조밀한 영역에서 새가 사라지고 섹션 경계에서 위치가 튀는 문제를 확인했다. 안전한 위치 탐색에 캔버스 양쪽 여백과 작은 크기를 추가하고, 커서 이동마다 목표 좌표를 갱신하되 GPU transform 전환으로 연속 이동하게 바꿨다. 텍스트·사진·컨트롤과의 간격을 검사하며, 동적 섹션 배치가 끝난 뒤 위치를 한 번 더 조정한다. 브라우저 테스트 7개와 실제 390px AR 슬라이더 반복 검증, `node --check` 및 `git diff --check`를 통과했다.
- 사용자 정정: 콘텐츠 겹침 금지는 섹션에 고정된 새에 관한 것이고 마우스 추종 새는 콘텐츠 위에 떠도 된다. 이 해석을 최우선으로 적용해 장애물 탐색·크기 축소·구간별 자동 숨김을 제거했다. 한 마리는 고정 크기 64px로 커서에서 18px 떨어져 사이트 캔버스 안에서 계속 움직이며, 클릭은 `pointer-events:none`으로 통과한다. 화면 바깥 여백에 마우스가 있어도 캔버스 가장자리에 붙어 보인다. 터치 입력일 때만 숨긴다. 새 추종 브라우저 테스트 8개, `node --check`, `git diff --check`를 통과했다.
- 로컬 Meta Pixel 작업과 최신 새 추종 작업의 병합에서 `index.html` 한 곳의 스크립트 태그 충돌을 해결했다. Meta 추적기와 새 추종 스크립트를 모두 유지하고 통합 `app.js` 캐시 버전을 적용했다.
- 320px·390px 실제 화면에서 가로 넘침이 없고 새 레이어가 로드되며 상세 → 가격 → 상담 복사 팝업 흐름이 유지되는 것을 확인했다. Meta 이벤트 테스트와 JavaScript 구문 검사는 통과했다. 새 추종 Python 테스트는 Edge 실행·임시 프로필 접근 권한 문제로 이 환경에서 재실행되지 않았다.
- 사용자의 배포 완료 알림 뒤 공개 `https://wistiahp.vercel.app/`에서 픽셀 ID `2825959717777272`, `meta-pixel.js`, 새 추종 스크립트, 통합 `app.js`가 실제 HTML에 포함된 것을 확인했다. AR 상세 → 가격 화면 이동과 상담 복사 팝업을 실제 공개 화면에서 검증했고 브라우저 오류 로그는 없었다. Meta 이벤트 관리자의 PageView·Contact 실수신과 서버 Conversions API 토큰 설정은 별도 확인이 필요하다.
- 2026-09-25 판매 광고 세트에서 표준 `Contact` 이벤트가 목표와 호환되지 않아 오류 `#2446814`가 발생하고, 사용자 지정 변환 생성 화면에서도 `Contact`를 기준 이벤트로 선택할 수 없는 것을 사용자 화면으로 확인했다. `모든 URL 트래픽` 기반 변환은 카카오톡 클릭과 다르므로 만들지 않았다.
- 기존 `Contact` 추적을 유지하면서 실제 카카오톡 채팅 링크 클릭 때 `KakaoTalkClick` 사용자 지정 이벤트를 브라우저 픽셀과 서버 Conversions API에 동일 이벤트 ID로 추가했다. Meta 서버 요청은 두 이벤트 수신을 검증하도록 고쳤고, `index.html`의 추적 스크립트 버전을 올렸다. 이벤트 테스트와 JavaScript 구문·Git 차이 검사를 통과했다.
- 로컬 390px 화면에서 AR 상세 → 가격 계산기 → 상담 복사 안내 팝업을 확인했고, 첫 상담 버튼만 눌렀을 때 카카오톡으로 이동하지 않는 동작을 유지했다. 320px 가격 화면에서 가로 넘침이 없었다. 로컬 미리보기는 Meta 전송이 차단되므로 새 사용자 지정 이벤트가 실제 Meta에 수신되고 판매 광고 세트에서 선택 가능한지는 운영 배포 후 확인해야 한다. 이번 작업은 원격 푸시·배포를 하지 않았다.
- 2026-09-25 운영 Meta 이벤트 관리자에서 `PageView`, `Contact`, `KakaoTalkClick`이 활성 수신 중임을 확인했다. 광고 계정 `1536278314908278`의 판매 광고 세트 전환 목표를 오류가 나던 `Contact`에서 `KakaoTalkClick`으로 변경해 초안에 저장했다.
- 시험 이벤트 화면에 이전 클릭이 보이지 않던 이유는 시험 코드가 없는 일반 운영 이벤트였기 때문이다. 임시 시험 코드 지원을 배포해 실제 사이트 클릭의 `Contact`·`KakaoTalkClick` 서버 이벤트가 시험 화면에서 `Deduplicated`로 표시됨을 확인했고, 별도 시험 요청은 두 이벤트가 `Processed`로 표시됐다. 검증 후 임시 URL 시험 코드 지원은 제거했다.
- 광고 세트는 아직 초안으로 유지했으며 이 시점에는 광고 소재가 없어 게시하지 않았다.
- 이후 동일 초안의 광고 소재로 기존 인스타그램 `wistia.film`의 2026-09-17 AR 축가 실제 본식 릴스(게시물 ID `18416679052156704`)를 선택했다. 행동 버튼은 `Learn more`, 목적지는 카카오톡 직행 주소에서 `https://wistiahp.vercel.app/#/detail/solo`로 교정해 홈페이지 방문 후 카카오톡 클릭을 추적할 수 있게 했다.
- 캠페인·광고 세트·광고 이름을 각각 `위스티아 AR 축가 | 판매 | 카카오톡 상담`, `AR 축가 | 카카오톡 이동 최적화`, `AR 축가 실제 본식 영상 | 홈페이지 연결`로 저장했다. 판매 목표·`KakaoTalkClick` 최적화·일일 예산 20,000원·대한민국 전체 타겟·종료일 없음·초안 상태를 확인했다. 집행 총액 한도가 정해지지 않아 게시하지 않았으며 광고비는 발생시키지 않았다.
- 사용자가 직접 이벤트 시험을 재현할 수 있도록 홈페이지 URL의 `meta_test_code=TEST...` 값을 카카오톡 클릭 시 서버 Conversions API의 `test_event_code`로 전달하게 했다. 일반 방문에는 시험 코드를 붙이지 않으며, 브라우저 픽셀이 없거나 오류를 내도 서버 이벤트 전송은 계속 시도한다.
- 추적 코드와 서버 요청의 자동 시험, `node --check`, `git diff --check`를 통과했다. 커밋 `88e47af`를 사용자의 기존 푸시 허가에 따라 `main`에 푸시했다. 운영 `meta-pixel.js?v=20260925-test-events`가 배포된 것을 확인하고 시험 코드 `TEST50039`가 있는 사이트에서 실제 카카오톡 버튼을 눌러 Meta 시험 화면에 새 `Contact`·`KakaoTalkClick` 서버 이벤트가 `Deduplicated`로 표시되는 것을 확인했다.
- 운영 사이트를 320px·390px으로 확인해 가로 넘침과 카카오톡 버튼 가림이 없었다. 사용자의 Chrome 시험 화면은 직접 접근할 수 없어 브라우저별 픽셀 차단 여부는 아직 확정하지 못했다. 광고 초안은 게시하지 않았다.
- 2026-09-25 사용자 지시에 따라 Meta 광고 계정 `1536278314908278`의 `AR 축가 | 카카오톡 이동 최적화` 광고 세트 일일 예산을 20,000원에서 10,000원으로 변경했다. 검토 화면에서 `Daily budget ₩10,000`과 `All edits saved`를 확인했다. 종료일은 없는 상태이며 광고는 초안으로 유지해 게시하거나 광고비를 발생시키지 않았다.

### 2026-09-26

- 첫 화면의 로고 오버레이·배경 및 서비스 카드 상승 연출과 상세 화면의 스크롤 등장 효과를 제거했다. 제목·사진·카드는 지연 없이 바로 표시된다.
- AR 상세 현장 영상의 별도 `소리 켜기` 버튼을 제거하고 재생·일시정지 버튼 하나로 영상과 소리를 함께 제어한다.
- 현장 영상, 보정 전후 비교, AR 비율 음원 구간을 스크롤 위치에 따라 자동 전환하고 이전 구간은 즉시 멈추도록 연결했다. 브라우저가 사용자 동작 전 소리 자동재생을 제한하는 경우에는 재생 버튼으로 시작할 수 있다.
- 로컬 미리보기에서 영상 → 보정 전후 비교 → AR 비율 음원으로 이동하며 이전 재생 정지와 다음 재생을 확인했다. 320px 첫 화면·390px 상세에서 가로 넘침이 없고 별도 소리 버튼이 표시되지 않음을 확인했다. `node --check`와 `git diff --check`를 통과했으며 원격 푸시·배포는 하지 않았다.
